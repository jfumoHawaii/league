name: Run R Script Daily

on:
  workflow_dispatch:   # lets you run manually (for testing)
  schedule:
    - cron: "0 14 * * *"   # runs daily at 9 AM Ecuador (UTC-5)

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up R
        uses: r-lib/actions/setup-r@v2

      - name: Install R packages
        run: |
          Rscript -e 'install.packages(c("httr","jsonlite","ggplot2","plotly","htmlwidgets","reshape2"))'

      - name: Run R script
        run: Rscript league.R

      - name: Upload outputs
        uses: actions/upload-artifact@v4
        with:
          name: outputs
          path: |
            *.png
            *.html
            *.pdf
            leagueScores.csv
